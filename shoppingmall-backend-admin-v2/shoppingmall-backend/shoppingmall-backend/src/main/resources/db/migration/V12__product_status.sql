-- ============================================================================
-- V12__product_status.sql
--
-- 상품 판매중지(SUSPENDED) 기능.
-- ddl-auto: update 환경(로컬)에서는 자동 반영되므로 실행 불필요.
-- validate 환경(운영/스테이징)에만 수동 적용.
-- ============================================================================

-- 상품 판매 상태. is_deleted(삭제)와 독립적인 축.
--   'ON_SALE'   : 판매중 (손님 목록/검색/상세 노출, 주문 가능)
--   'SUSPENDED' : 판매자 일시 판매중지 (판매자 본인 목록에만 보이고 손님에겐 감춰짐)
-- 기존 상품은 전부 판매중으로 간주한다.
ALTER TABLE products
    ADD COLUMN status VARCHAR(20) NOT NULL DEFAULT 'ON_SALE';
